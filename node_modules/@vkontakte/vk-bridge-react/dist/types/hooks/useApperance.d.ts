import type { AppearanceType } from '@vkontakte/vk-bridge';
export type UseAppearance = AppearanceType | null;
/**
 * Note: it works only for "embedded" app mode.
 */
export declare const useAppearance: () => UseAppearance;
