import type { Insets } from '@vkontakte/vk-bridge';
export type UseInsets = {
    top: Insets['top'];
    right: Insets['right'];
    bottom: Insets['bottom'];
    left: Insets['left'];
} | null;
export declare const useInsets: () => UseInsets;
