import * as React from 'react';
import { HTMLAttributesWithRootRef } from '../../types';
export interface PullToRefreshSpinnerProps extends HTMLAttributesWithRootRef<HTMLDivElement> {
    size?: number;
    strokeWidth?: number;
    on?: boolean;
    progress?: number;
}
export declare const PullToRefreshSpinner: ({ on, size, strokeWidth, progress, children, ...restProps }: PullToRefreshSpinnerProps) => React.JSX.Element;
//# sourceMappingURL=PullToRefreshSpinner.d.ts.map