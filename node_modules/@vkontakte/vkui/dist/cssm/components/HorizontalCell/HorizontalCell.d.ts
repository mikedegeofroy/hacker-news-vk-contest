import * as React from 'react';
import { HasRef, HasRootRef } from '../../types';
import { type TappableProps } from '../Tappable/Tappable';
export interface HorizontalCellProps extends Omit<TappableProps, 'size' | 'getRootRef'>, HasRootRef<HTMLDivElement>, HasRef<HTMLDivElement> {
    size?: 's' | 'm' | 'l';
    /**
     * Заголовок
     */
    header?: React.ReactNode;
    /**
     * Дополнительная строка текста под `children`.
     */
    subtitle?: React.ReactNode;
    /**
     * Дополнительная строка текста под `children` и `subtitle`.
     */
    extraSubtitle?: React.ReactNode;
}
/**
 * @see https://vkcom.github.io/VKUI/#/HorizontalCell
 */
export declare const HorizontalCell: ({ className, header, style, subtitle, size, children, getRootRef, getRef, extraSubtitle, ...restProps }: HorizontalCellProps) => React.JSX.Element;
//# sourceMappingURL=HorizontalCell.d.ts.map