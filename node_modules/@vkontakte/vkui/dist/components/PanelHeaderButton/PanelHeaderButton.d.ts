import * as React from 'react';
import { TappableProps } from '../Tappable/Tappable';
export interface PanelHeaderButtonProps extends Omit<TappableProps, 'label'> {
    primary?: boolean;
    label?: React.ReactNode;
}
/**
 * @see https://vkcom.github.io/VKUI/#/PanelHeaderButton
 */
export declare const PanelHeaderButton: ({ children, primary, label, className, ...restProps }: PanelHeaderButtonProps) => React.JSX.Element;
//# sourceMappingURL=PanelHeaderButton.d.ts.map