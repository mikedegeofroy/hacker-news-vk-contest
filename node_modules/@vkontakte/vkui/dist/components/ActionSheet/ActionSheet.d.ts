import * as React from 'react';
import { SharedDropdownProps } from './types';
type CloseInitiators = 'action-item' | 'cancel-item' | 'other';
export interface ActionSheetOnCloseOptions {
    closedBy: CloseInitiators;
}
export interface ActionSheetProps extends Pick<SharedDropdownProps, 'toggleRef' | 'popupOffsetDistance' | 'placement'>, React.HTMLAttributes<HTMLDivElement> {
    header?: React.ReactNode;
    text?: React.ReactNode;
    /**
     * Закрыть попап по клику снаружи.
     */
    onClose(options: ActionSheetOnCloseOptions): void;
    /**
     * Только мобильный iOS.
     */
    iosCloseItem?: React.ReactNode;
    mode?: 'sheet' | 'menu';
}
/**
 * @see https://vkcom.github.io/VKUI/#/ActionSheet
 */
export declare const ActionSheet: ({ children, className, header, text, style, iosCloseItem, popupOffsetDistance, placement, mode: modeProp, ...restProps }: ActionSheetProps) => React.JSX.Element;
export {};
//# sourceMappingURL=ActionSheet.d.ts.map