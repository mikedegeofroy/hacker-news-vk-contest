import { BrowserSymbol } from './browser-symbol';
export interface SpriteConfig {
    attrs?: Record<string, string>;
}
export declare class BrowserSprite {
    private symbols;
    private config;
    node: Element | null;
    constructor(config?: SpriteConfig);
    private push;
    add(symbol: BrowserSymbol): boolean;
    attach(target: Element): void;
    mount(target: Node, prepend?: boolean): Element;
    private render;
    unmount(): void;
}
