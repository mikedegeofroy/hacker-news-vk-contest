/**
 * For browser-based histories, we combine the state and key into an object
 */
export function getHistoryState(location, index) {
    return {
        usr: location.state,
        key: location.key,
        idx: index,
    };
}
//# sourceMappingURL=getHistoryState.js.map