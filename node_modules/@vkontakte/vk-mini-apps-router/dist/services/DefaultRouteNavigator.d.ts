import { BlockerFunction, Params, Router } from '@remix-run/router';
import { NavigationOptions, RouteNavigator } from './RouteNavigator.type';
import { Page, PageWithParams } from '../page-types/common';
import { ViewHistory } from './ViewHistory';
import { TransactionExecutor } from './TransactionExecutor';
export declare class DefaultRouteNavigator implements RouteNavigator {
    private viewHistory;
    private transactionExecutor;
    private readonly router;
    private readonly setPopout;
    private blockers;
    private blockerId;
    constructor(router: Router, viewHistory: ViewHistory, transactionExecutor: TransactionExecutor, setPopout: (popout: JSX.Element | null) => void);
    push(to: string | Page | PageWithParams<string>, paramsOrOptions?: Params | NavigationOptions, options?: NavigationOptions): Promise<void>;
    replace(to: string | Page | PageWithParams<string>, paramsOrOptions?: Params | NavigationOptions, options?: NavigationOptions): Promise<void>;
    back(to?: number): Promise<void>;
    backToFirst(): Promise<void>;
    go(to: number): Promise<void>;
    runSync(actions: VoidFunction[]): Promise<void>;
    showModal(id: string): Promise<void>;
    hideModal(pushPanel?: boolean): Promise<void>;
    showPopout(popout: JSX.Element): Promise<void>;
    hidePopout(): Promise<void>;
    block(blocker: BlockerFunction): () => void;
    private navigate;
}
//# sourceMappingURL=DefaultRouteNavigator.d.ts.map